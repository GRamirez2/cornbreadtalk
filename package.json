{
	"name": "cornbreadtalk",
	"version": "0.1.0",
	"private": false,
	"scripts": {
		"dev": "next dev",
		"build": "next build",
		"start": "next start",
		"export": "next export",
		"deploy": "npm run build && npm run export && aws2 s3 cp out/ s3://cornbreadtalk.com --recursive"
	},
	"dependencies": {
		"next": "11.1.0",
		"react": "16.12.0",
		"react-dom": "16.12.0"
	}
}
